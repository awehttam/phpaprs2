# Claude Code Instructions

## Service Worker Cache Management

When updating frontend files (those listed in ASSETS_TO_CACHE within sw.js) update the CACHE_NAME version in sw.js to ensure changed assets are loaded.
